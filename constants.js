const HEDERA_CREDENTIALS_NOT_FOUND = "Unable to load hedera credentials";
const TOKEN_NAME = "seun_awonugba_token";
const MAX_RETRIES = 10;

module.exports = {
    HEDERA_CREDENTIALS_NOT_FOUND,
    TOKEN_NAME,
    MAX_RETRIES,
};
